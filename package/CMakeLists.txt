# Optional C++ example (FileReader) — not needed for a QML-only plasmoid.
# Uncomment if you want to build/use it as a starting point for your own
# C++-backed QML type. See package/plugin/ and README.md "C++ plugin".
# add_subdirectory(plugin)
